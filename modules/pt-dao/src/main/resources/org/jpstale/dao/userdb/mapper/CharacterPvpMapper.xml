<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jpstale.dao.userdb.mapper.CharacterPvpMapper">

    <!-- C++ pvpserver.cpp -->
    <select id="selectTop100CharacterNameByKills" resultType="java.lang.String">
        SELECT character_name FROM userdb.characterpvp WHERE kills &gt; 0 ORDER BY kills DESC LIMIT 100
    </select>
    <update id="updateTopPvpByCharacterName">
        UPDATE userdb.characterpvp SET top_pvp = 1 WHERE character_name = #{characterName}
    </update>
    <update id="resetAllKillsDeaths">
        UPDATE userdb.characterpvp SET kills = 0, deaths = 0, kill_streak = 0, top_pvp = 0
    </update>
    <select id="selectIdByCharacterName" resultType="java.lang.Integer">
        SELECT id FROM userdb.characterpvp WHERE character_name = #{characterName}
    </select>
    <select id="selectKillsDeathsTopPvpByCharacterName" resultType="org.jpstale.dao.userdb.entity.CharacterPvp">
        SELECT kills, deaths, top_pvp FROM userdb.characterpvp WHERE character_name = #{characterName}
    </select>
    <update id="updateTopPvp0ByCharacterName">
        UPDATE userdb.characterpvp SET top_pvp = 0 WHERE character_name = #{characterName}
    </update>
    <update id="updateKillsDeathsKillStreakByCharacterName">
        UPDATE userdb.characterpvp SET kills = #{kills}, deaths = #{deaths}, kill_streak = #{killStreak} WHERE character_name = #{characterName}
    </update>
    <insert id="insertCharacterPvp" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO userdb.characterpvp (character_name, kills, deaths, kill_streak, top_pvp) VALUES (#{characterName}, #{kills}, #{deaths}, #{killStreak}, 0)
    </insert>
    <!-- characterserver rename -->
    <update id="updateCharacterNameByCharacterName">
        UPDATE userdb.characterpvp SET character_name = #{newName} WHERE character_name = #{oldName}
    </update>

</mapper>
