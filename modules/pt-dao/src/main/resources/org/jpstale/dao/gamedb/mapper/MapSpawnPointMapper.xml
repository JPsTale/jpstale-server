<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jpstale.dao.gamedb.mapper.MapSpawnPointMapper">

    <!-- C++ unitinfo.cpp -->
    <select id="selectCountByStage" resultType="java.lang.Integer">
        SELECT COUNT(id) FROM gamedb.mapspawnpoint WHERE stage = #{stage}
    </select>
    <select id="selectByStage" resultType="org.jpstale.dao.gamedb.entity.MapSpawnPoint">
        SELECT * FROM gamedb.mapspawnpoint WHERE stage = #{stage}
    </select>
    <select id="selectCountByStageAndXAndZ" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM gamedb.mapspawnpoint WHERE stage = #{stage} AND x = #{x} AND z = #{z}
    </select>
    <select id="selectTop1IdOrderByIdDesc" resultType="java.lang.Integer">
        SELECT id FROM gamedb.mapspawnpoint ORDER BY id DESC LIMIT 1
    </select>
    <insert id="insertMapSpawnPoint">
        INSERT INTO gamedb.mapspawnpoint (id, stage, x, z, description) VALUES (#{id}, #{stage}, #{x}, #{z}, #{description})
    </insert>
    <delete id="deleteByXAndZ">
        DELETE FROM gamedb.mapspawnpoint WHERE x = #{x} AND z = #{z}
    </delete>
    <!-- mapserver.cpp:3651 -->
    <select id="selectXZByStage" resultType="org.jpstale.dao.gamedb.entity.MapSpawnPoint">
        SELECT x, z FROM gamedb.mapspawnpoint WHERE stage = #{stage}
    </select>

</mapper>
