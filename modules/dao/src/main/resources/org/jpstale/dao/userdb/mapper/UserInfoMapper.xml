<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jpstale.dao.userdb.mapper.UserInfoMapper">

    <resultMap id="UserInfoMap" type="org.jpstale.dao.userdb.entity.UserInfo">
        <id property="id" column="id"/>
        <result property="accountName" column="accountname"/>
        <result property="password" column="password"/>
        <result property="regisDay" column="regisday"/>
        <result property="flag" column="flag"/>
        <result property="active" column="active"/>
        <result property="activeCode" column="activecode"/>
        <result property="coins" column="coins"/>
        <result property="email" column="email"/>
        <result property="gameMasterType" column="gamemastertype"/>
        <result property="gameMasterLevel" column="gamemasterlevel"/>
        <result property="gameMasterMacAddress" column="gamemastermacaddress"/>
        <result property="coinsTraded" column="coinstraded"/>
        <result property="banStatus" column="banstatus"/>
        <result property="unbanDate" column="unbandate"/>
        <result property="isMuted" column="ismuted"/>
        <result property="muteCount" column="mutecount"/>
        <result property="unmuteDate" column="unmutedate"/>
    </resultMap>

    <!-- C++ accountserver.cpp:298 - SELECT TOP(1) UserInfo by AccountName -->
    <select id="selectOneByAccountName" resultMap="UserInfoMap">
        SELECT id, accountname, password, flag, active, coins, gamemastertype, gamemasterlevel,
               gamemastermacaddress, banstatus, unbandate, ismuted, mutecount, unmutedate
        FROM userdb.userinfo WHERE accountname = #{accountName}
        LIMIT 1
    </select>

    <!-- 注册查重：按邮箱 -->
    <select id="selectOneByEmail" resultType="org.jpstale.dao.userdb.entity.UserInfo">
        SELECT id, accountname, email FROM userdb.userinfo WHERE email = #{email}
        LIMIT 1
    </select>

    <!-- C++ accountserver.cpp:758 -->
    <select id="selectIsMutedMuteCountByAccountName" resultType="org.jpstale.dao.userdb.entity.UserInfo">
        SELECT ismuted, mutecount FROM userdb.userinfo WHERE accountname = #{accountName}
    </select>

    <!-- C++ accountserver.cpp:786 -->
    <select id="selectGameMasterByAccountName" resultType="org.jpstale.dao.userdb.entity.UserInfo">
        SELECT gamemastertype, gamemasterlevel FROM userdb.userinfo WHERE accountname = #{accountName}
    </select>

    <!-- C++ accountserver.cpp:819 -->
    <select id="selectFlagByAccountName" resultType="org.jpstale.dao.userdb.entity.UserInfo">
        SELECT flag FROM userdb.userinfo WHERE accountname = #{accountName}
    </select>

    <!-- C++ accountserver.cpp:853,902,930,955 - unban / unmute -->
    <update id="updateUnbanById">
        UPDATE userdb.userinfo SET banstatus = 0, unbandate = NULL WHERE id = #{id}
    </update>
    <update id="updateUnbanByAccountName">
        UPDATE userdb.userinfo SET banstatus = 0, unbandate = NULL WHERE accountname = #{accountName}
    </update>
    <update id="updateUnmuteByAccountName">
        UPDATE userdb.userinfo SET ismuted = 0, unmutedate = NULL WHERE accountname = #{accountName}
    </update>
    <update id="updateUnmuteById">
        UPDATE userdb.userinfo SET ismuted = 0, unmutedate = NULL WHERE id = #{id}
    </update>

    <!-- C++ accountserver.cpp:250,259 - decrement mute -->
    <update id="updateUnmuteDecrementMuteById">
        UPDATE userdb.userinfo SET ismuted = 0, mutecount = (mutecount - 1), unmutedate = NULL WHERE id = #{id}
    </update>

    <!-- C++ accountserver.cpp:4993 servercommand - mute user -->
    <update id="updateMuteById">
        UPDATE userdb.userinfo SET ismuted = 1, mutecount = #{muteCount},
               unmutedate = CURRENT_TIMESTAMP + (#{hours} * INTERVAL '1 hour') WHERE id = #{id}
    </update>

    <!-- C++ logserver.cpp:758, cheatserver.cpp:36 -->
    <update id="updateBanStatusByAccountName">
        UPDATE userdb.userinfo SET banstatus = #{banStatus}, unbandate = NULL WHERE accountname = #{accountName}
    </update>

    <!-- C++ userserver.cpp:137 - select ID by AccountName -->
    <select id="selectIdByAccountName" resultType="java.lang.Integer">
        SELECT id FROM userdb.userinfo WHERE accountname = #{accountName}
    </select>

    <!-- C++ CoinShopHandler - coins -->
    <select id="selectCoinsByAccountName" resultType="java.lang.Integer">
        SELECT coins FROM userdb.userinfo WHERE accountname = #{accountName}
    </select>
    <select id="selectCoinsTradedByAccountName" resultType="java.lang.Integer">
        SELECT coinstraded FROM userdb.userinfo WHERE accountname = #{accountName}
    </select>
    <update id="updateCoinsDecrementByAccountName">
        UPDATE userdb.userinfo SET coins = coins - #{amount} WHERE accountname = #{accountName}
    </update>
    <update id="updateCoinsTradedIncrementByAccountName">
        UPDATE userdb.userinfo SET coinstraded = coinstraded + #{amount} WHERE accountname = #{accountName}
    </update>
    <update id="updateCoinsIncrementByAccountName">
        UPDATE userdb.userinfo SET coins = coins + #{amount} WHERE accountname = #{accountName}
    </update>

</mapper>
