<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jpstale.dao.gamedb.mapper.ModelListMapper">

    <!-- C++ unitinfo.cpp -->
    <select id="selectIdByModel" resultType="java.lang.Integer">
        SELECT id FROM gamedb.modellist WHERE model = #{model}
    </select>
    <select id="selectDeathModelIdByModel" resultType="java.lang.Integer">
        SELECT death_model_id FROM gamedb.modellist WHERE model = #{model}
    </select>
    <select id="selectWidthHeightById" resultType="org.jpstale.dao.gamedb.entity.ModelList">
        SELECT width, height FROM gamedb.modellist WHERE id = #{id}
    </select>
    <select id="selectModelById" resultType="java.lang.String">
        SELECT model FROM gamedb.modellist WHERE id = #{id}
    </select>
    <insert id="insertModelList" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO gamedb.modellist (model, width, height, death_model_id) VALUES (#{model}, #{width}, #{height}, #{deathModelId})
    </insert>
    <select id="selectTop1IdOrderByIdDesc" resultType="java.lang.Integer">
        SELECT id FROM gamedb.modellist ORDER BY id DESC LIMIT 1
    </select>

</mapper>
